{
  "name": "hello-parcel",
  "version": "1.0.0",
  "description": "Project to demonstrate parcel usage",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "api": "json-server --watch db.json",
    "serve": "run-p start api",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ricmello",
  "license": "MIT",
  "devDependencies": {
    "json-server": "^0.17.0",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.7.0"
  }
}
