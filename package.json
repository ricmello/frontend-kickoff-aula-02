{
  "name": "hello-parcel",
  "version": "1.0.0",
  "description": "Project to demonstrate parcel usage",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ricmello",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.7.0"
  }
}
